<div class="add_entity"><%= link_to 'добавить фильм', :controller => 'entities', :action => 'add' %><br /></div>
<div class="search">
  <% form_for :user, :url => {:controller => 'entities', :action => 'search'} do %>
        <%= text_field_tag :word, params[:word] %>
        <%= submit_tag "Поиск", :class => 'submit' %>
  <% end %>
</div>
<br>

<%= show_entities @entities %>

<% if @entities.empty? %>
  <h1>Тут пока пусто.</h1> <h2> Зарегистрируйтесь и добавьте новый фильм.</h2>
<% end %>

<!-- Постраничная навигация -->
<div class="navigator">
  <%= @next_page ? link_to('|&larr;', :action => 'index') : '' %>
  <%= @dnext_page ? link_to(@dnext_page, :action => 'index', :page => @dnext_page) : '' %>
  <%= @next_page ? link_to(@next_page, :action => 'index', :page => @next_page) : '' %>
  <%= @cur_page %>
  <%= @prev_page ? link_to(@prev_page, :action => 'index', :page => @prev_page) : '' %>
  <%= @dprev_page ? link_to(@dprev_page, :action => 'index', :page => @dprev_page) : '' %>
  <%= @prev_page ? link_to('&rarr;|', :action => 'index', :page => 1) : '' %>
</div>

<%= will_paginate @entities2 %>